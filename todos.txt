1.  Address addgrv errata as follows:
  a.  Fix ordering of the processing of multiple groves to avoid memory overload.
  b.  Add --help option.
  c.  Use ordered sets instead of unordered sets for ease of testing.
  d.  Extract memos and add to session table.

2.  Create utility to generate data dictionaries.

3.  Create utility to generate commands needed to replicate the desired model.
